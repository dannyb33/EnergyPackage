name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - math
  - copy
  - networkx

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

